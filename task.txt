# Professional Security Monitor v2 - Enterprise Grade Windows Hardening

You are a Senior Security Engineer at Microsoft with 15+ years experience in Windows EDR/EDR development, PowerShell internals, and threat hunting. Your task is to transform this amateur security monitor into a **production-ready, enterprise-grade tool** that achieves **95% persistence/malware detection coverage** suitable for GitHub trending and enterprise deployment.

## Current Codebase Analysis
setup.ps1 (provided below) - installs Python deps, interactive Gmail config (plain-text JSON), creates scheduled task (Limited privileges), basic test run
security_check.py (not provided but described) - 8 basic checks: Chrome extensions, Run keys, processes, network, hosts, AI configs, Defender, Scheduled Tasks
config.json - stores Gmail app password in plaintext
README.md - good UX but lacks enterprise features

## REQUIRED TRANSFORMATIONS (95% Coverage)

### 1. setup.ps1 → Enterprise Installer
CRITICAL CHANGES:
├── Replace plaintext config.json → Windows Credential Manager (cmdkey /generic)
├── SYSTEM principal + Highest privileges for scheduled task
├── Generate baseline.json: Chrome extensions, startup folders, auto services, known processes, scheduled tasks snapshot
├── Script self-integrity: SHA256 hash of security_check.py saved to secure config
├── BitLocker detection + warning if not enabled
├── Windows Event Log integration for audit trail
├── Multi-stage validation: admin check → Python check → network test → baseline creation → task registration → E2E test
├── Professional error handling with structured JSON logging
└── Self-healing: if task missing/corrupted, recreate during check


### 2. security_check.py → Production EDR Engine (15+ Checks)
EXPANDED DETECTION COVERAGE (95% MITRE ATT&CK T1037/T1543 coverage):
EXISTING 8 CHECKS (keep + enhance):
├── Chrome Extensions → baseline comparison + permission risk scoring
├── Registry Run keys → hash checking + unusual paths
├── Running Processes → signed/unsigned + unusual paths + child processes
├── Network → C2 patterns + direct IP + TOR exit nodes
├── Hosts file → DNS hijacking
├── AI configs → prompt injection signatures
├── Windows Defender → service status + exclusions
└── Scheduled Tasks → hidden persistence

NEW 7+ CHECKS (CRITICAL for 95% coverage):
├── Windows Services (Auto/Delayed) → unusual paths + unsigned binaries
├── Startup Folders (User/AllUsers) → new/changed .lnk/.exe
├── WMI Persistence → EventFilters/Consumers with suspicious queries
├── PowerShell Profiles → execution policy + profile tampering
├── BITS Jobs → download cradles
├── Prefetch anomalies → unusual execution patterns
├── Self-integrity → script hash + scheduled task validation
├── Baseline drift detection → ANY new/changed items flagged
└── Windows Event Log → security events last 24h


### 3. Professional Testing Framework
E2E VALIDATION PIPELINE:
├── --test mode → simulates 5 threat types (miner.exe, malicious extension, WMI persistence, service, startup item)
├── --validate-install → full baseline + integrity validation
├── --doctor → diagnoses common install issues (Python path, task permissions, network, CredMan)
├── Automated smoke tests during setup (task creation → execution → email → notification)
└── Test report generation (JSON + human-readable)


### 4. Enterprise README & Documentation
PROFESSIONAL DOCUMENTATION:
├── Architecture diagram (Mermaid/PlantUML)
├── Threat model + MITRE ATT&CK coverage matrix
├── Installation video/gif embed
├── Comparison table vs commercial EDR (Sysmon, CarbonBlack)
├── Configuration guide (Paranoid/Standard/Light modes)
├── Troubleshooting matrix
├── Contribution guidelines
├── Security.txt + vulnerability disclosure
└── Enterprise deployment (GPO, Intune)


## IMPLEMENTATION REQUIREMENTS

**Output Format:**
[setup.ps1] - Complete enterprise installer (600-800 lines)

[security_check.py] - Production EDR engine (1200-1500 lines, 15+ functions)

[test_security.py] - Automated test framework

[baseline.json] - Schema example

[README.md] - Professional 3-page documentation

[IMAGES] - Architecture diagram, install flow, sample alerts

[.github/workflows] - CI/CD pipeline

[CHANGELOG.md] - Versioned release notes

**Technical Standards:**
- Zero false positives in test mode
- <30s execution time (95th percentile) 
- Structured JSON logging + human-readable console
- Graceful degradation (email failed → file log → notification)
- Idempotent installation (safe re-runs)
- Windows 10/11, Server 2019/2022 compatible
- PowerShell 5.1+, Python 3.8+ required (checked)
- MIT License, enterprise-ready

**Security Standards:**
- No plaintext secrets (CredMan only)
- Script signing validation
- Anti-tampering (hash checking)
- Secure defaults (deny by default)
- Escape all shell execution
- Input validation everywhere

## Current setup.ps1 (BASELINE)
[Paste your full setup.ps1 code here]


## Success Criteria (95% Coverage Validation)
✅ 15+ persistence vectors covered (MITRE ATT&CK validated)
✅ Credential Manager integration (no plaintext secrets)
✅ Baseline drift detection (zero-day capable)
✅ Self-healing + tamper resistance
✅ E2E test automation (5 threat scenarios)
✅ Professional README (architecture, comparisons, metrics)
✅ GitHub-ready (CI/CD, changelog, security.txt)
✅ <30s execution, <50MB disk, zero dependencies beyond Python stdlib+psutil
✅ Enterprise documentation (GPO/Intune ready)


**Generate COMPLETE working code that passes all tests and is ready for GitHub release v2.0.0.** 
